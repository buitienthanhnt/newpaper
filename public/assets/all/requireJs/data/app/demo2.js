define(['require', 'jquery', 'knockout'], function (require, $, ko) { // khai báo như này không được đặt tên nếu không sẽ bị lỗi.
	var p1 = $('#platform');
	// console.log(p1.html());
	return {
		getName: function () {
			return 21342134234;
		}
	};
});